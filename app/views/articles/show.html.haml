%h1= @article.title
%p= @article.content
.btn-group
- if user_signed_in?
	= link_to "Back", root_path, :class => "btn btn-default"
	= link_to "Edit", edit_article_path(@article), :class => "btn btn-info"
	= link_to "Destroy", article_path(@article),:method=>:destroy, :class=>"btn btn-danger"